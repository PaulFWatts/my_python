

class Pet:
    def __init__(se1f, name, species):
        self.name = name
        Self.species = species
    def getName(se1f):
        return Self.name
    def getSpecies(self):
        return self.species
    def __str__(se1f):
        return ("%s is a %s") % (self.name, self.species)


davesPet = Pet("Kitty", "Cat")  